variables:
- group: common
- template: /.azuredevops/variables-global.yml@pipelines_repo

parameters:
- name: pipelinesRepoRef
  type: string
  default: refs/heads/develop
- name: triggerDownstreamJobs
  type: boolean
  default: true

resources:
  repositories:
  - repository: pipelines_repo
    type: github
    endpoint: ROCm
    name: ROCm/ROCm
    ref: ${{ parameters.pipelinesRepoRef }}

trigger:
  batch: true
  branches:
    include:
    - develop
    - release-staging/rocm-rel-7.*
  paths:
    include:
    - projects/hipblas
    exclude:
    - projects/hipblas/.githooks
    - projects/hipblas/.github
    - projects/hipblas/.jenkins
    - projects/hipblas/docs
    - projects/hipblas/.*.y*ml
    - projects/hipblas/*.md

pr: none

stages:
- stage: hipblas
  jobs:
  - template: ${{ variables.CI_COMPONENT_PATH }}/hipBLAS.yml@pipelines_repo
    parameters:
      sparseCheckoutDir: projects/hipblas
      triggerDownstreamJobs: ${{ parameters.triggerDownstreamJobs }}
- template: templates/report-summary-check-wrapper.yml
